#!/usr/bin/env python## coding: utf-8import os, sysimport math#コサイン類似度計算用関数def dotProduct(dicX,dicY):	"""return a dot product."""	sum = 0	for key in dicX:		if key in dicY:			sum += float(dicX[key])*float(dicY[key])	return sum#コサイン類似度計算用関数def root_squareSum(vector):	"""the root of the sum of squares"""	return math.sqrt(sum([x**2 for x in vector]))#コサイン類似度計算用関数def cosineSim(dicX,dicY):	"""this is a main"""	if dicX=={} or dicY=={}:		return 0	upper = dotProduct(dicX,dicY)	bottom = root_squareSum(dicX.values()) * root_squareSum(dicY.values())	return float(upper)/bottom#コサイン類似度計算用関数def Cosine(dic_tf_idf):	dic_result = {}	for dicX in dic_tf_idf.keys():		for dicY in dic_tf_idf.keys():			if dicX != dicY:				if cosineSim(dic_tf_idf[dicX], dic_tf_idf[dicY]) != 0.0:					dic_result[str(dicX) + ":" + str(dicY)] = cosineSim(dic_tf_idf[dicX], dic_tf_idf[dicY])	return dic_result#tf-idf値が入った辞書dic_tf_idfを引数とするif __name__ == "__main__":	print "標準入力からは使えません"