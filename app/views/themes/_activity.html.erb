<div class="panel panel-default">
  <div class="panel-body">
    <h3><%= t('themes.activity') %></h3>
    <% @activities.each do |activity| %>
      <%= activity.created_at %>
      <%= activity.atype %>
      <br />
      <%= activity.entry.body %>
      <br />
    <% end %>
  </div>
</div>
