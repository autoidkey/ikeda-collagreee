<div class="panel panel-default">
  <div class="panel-body">
    <h3>Other theme / 他のテーマ</h3>
    <% @other_themes.each do |theme| %>
      <%= link_to theme.title, theme_path(theme) %>
    <% end %>
  </div>
</div>
