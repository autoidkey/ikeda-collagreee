<h3><%= @theme.title %></h3>

<h5>new entry</h5>
<%= render 'entry_form' %>



<br />

<% @entries.each do |theme| %>
  <%= theme.title %>
  <%= theme.body %>

  <%= link_to 'show', theme %>
  <br />
<% end %>
