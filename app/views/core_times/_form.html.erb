<%= form_for(@core_time) do |f| %>
  <% if @core_time.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@core_time.errors.count, "error") %> prohibited this core_time from being saved:</h2>

      <ul>
      <% @core_time.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :theme_id %><br>
    <%= f.select :theme_id, @themes.map{|r| [r.title, r.id]} %>
  </div>
  <div class="field">
    <%= f.label :start_at %><br>
    <%= f.datetime_select :start_at %>
  </div>
  <div class="field">
    <%= f.label :end_at %><br>
    <%= f.datetime_select :end_at %>
  </div>
  <div class="field">
    <%= f.label :notice %><br>
    <%= f.check_box :notice %>
  </div>
  <div class="">
    <%= f.submit '作成', class: 'btn btn-default' %>
  </div>
<% end %>
